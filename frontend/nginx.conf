server {
    listen ${PORT};
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback so /greeting/<token> resolves to React Router
    location / {
        try_files $uri /index.html;
    }
}
